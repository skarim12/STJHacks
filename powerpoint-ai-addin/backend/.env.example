# Backend env vars
# PORT=3000

# --- Images / Visuals ---
# Pexels API key (stock photos). If not set, the backend will skip Pexels.
PEXEL_API=

# OpenAI key (used for LLM fallback + OPTIONAL image generation fallback)
API_KEY=
# OPENAI_IMAGE_MODEL=gpt-image-1

# --- LLM keys (either can be set) ---
CLAUDE_API_KEY=
# ANTHROPIC_API_KEY=...

# Optional model overrides
OPENAI_MODEL=
# Recommended Anthropic model (override if your account differs)
CLAUDE_MODEL=claude-3-5-sonnet-20241022
